{
  "name": "cover-letter-ai",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "netlify functions:serve & serve public",
    "build": "npm install",
    "test": "jest"
  },
  "dependencies": {
    "node-fetch": "^3.3.2",
    "serve": "^14.2.1",
    "@netlify/functions": "^1.4.0"
  },
  "devDependencies": {
    "netlify-cli": "^16.0.1",
    "jest": "^29.7.0",
    "eslint": "^8.56.0"
  },
  "jest": {
    "testEnvironment": "node",
    "coveragePathIgnorePatterns": [
      "/node_modules/",
      "/netlify/functions/"
    ]
  }
}
